{
    "version": 2,
    "builds": [
      { "src": "Dockerfile", "use": "@vercel/docker" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" }
    ],
    "rewrites": [
      { "source": "/api/(.*)", "destination": "/api/$1" }
    ]
  }